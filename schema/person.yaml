type: object
additionalProperties: false
required:
  - id
  - name
properties:
  id:
    type: string
    pattern: "^[a-z0-9-]+$"
  name:
    type: string
  links:
    type: object
    additionalProperties:
      type: string
      format: uri